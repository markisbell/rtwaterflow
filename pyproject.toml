[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "rtwaterflow"
version = "0.0.1"
description = "Realtime pandapipes drinking-water network simulator: hydraulics with elevation, live map, three-layer observability"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Markus Bell" }]
dependencies = [
    "pandapipes==0.14.0",
    "numpy>=1.24",
    "pandas>=2.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
]

[project.optional-dependencies]
# httpx2: starlette >= 1.3 TestClient prefers it over httpx (deprecation)
# wntr: EPANET cross-validation + tank/hydrant oracles (test-only, importorskip-
# gated); belongs here NOT in requirements.txt so it stays out of the runtime image
dev = ["pytest>=8.0", "httpx>=0.27", "httpx2", "wntr>=1.5"]

[project.scripts]
rtwaterflow = "rtwaterflow.main:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
